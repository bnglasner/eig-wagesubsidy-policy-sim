# Handoff Prompt: Holistic Evaluation of the Entry Model (for a fresh thread)

**Written:** 2026-07-09, end of the remodel session. Paste the block below into `/orchestrate` in a new conversation.

---

Holistically evaluate the entry-from-nonemployment modeling in this repo (the 80-80 wage-subsidy simulation). This is a NEW thread with NO memory of the sessions that built what you are evaluating — read the files, verify against live outputs, and do not trust this summary where it conflicts with the repo. You are being asked to challenge a build that has been revised three times in two days, each revision correct in isolation; your job is to judge whether the ACCUMULATED structure is coherent, defensibly centered, and publication-ready — and to be equally willing to conclude it now over-corrects or under-corrects. Do NOT write or modify analysis code until your evaluation report has been reviewed.

=== PROJECT BASICS ===
Read `PROJECT.md`, `Infrastructure/GUARDRAILS.md`, `Infrastructure/AI_WORKFLOW.md`, `.claude/agents/orchestrator.md` first, per repo convention. The policy: the 80-80 subsidy fills 80 percent of the gap between a worker's employer-paid wage and a target = 80 percent of the national paid-hourly median ($21.00 → $16.80), per paycheck, no phase-out, capped at 40 hrs/week, base wage $7.25. Environment: Python venv at `.venv/bin/python` (pandas/numpy/statsmodels/scipy/pyarrow/pytest); R 4.5.2 with the EIG figure theme; every stage runs standalone in seconds; the pipeline is deterministic (hash-based draws, byte-identical reruns). Run `python -m pytest tests/` (static parity) before and after anything.

=== CURRENT ENTRY-MODEL ARCHITECTURE (verify each layer against code) ===
1. **Pool** (`code/01_data_preparation/01h_nonemployed_pool.py` + `01i_household_links.py`): all non-employed 16–64 from raw ORG rosters, 59.28M weighted = 7.0M unemployed / 33.8M NILF-other (16.7M aged 16–24) / 9.7M disabled / 8.7M retired (`prior_status`). Person keys; real in-household spouse-employment links; child-under-5 flags.
2. **Potential wages**: Heckman two-step on the PAID-HOURLY frame (estimation sample = non-employed + paid-hourly earners only; salaried excluded; ρσ ≈ +0.075 — the earlier all-earner frame's +0.30 was shown to be salaried contamination). Conditional-Mills (D=0) prediction, Duan smearing, then **group-specific offer dispersion**: mean-preserving lognormal spread, σ by education×age from own residuals (0.29–0.55), scaled λ=0.75 (headline; nets out ~CPS measurement error), hash-quantile draws salted independently of the entry lottery. Result: 41.4% of pool draws offers < $16.80 vs 23.6% of paid-hourly workers.
3. **Entry decision**: net-gain-space threshold — enter iff NI(y·h + subsidy) − NI(0) ≥ (1+m_i)·max(NI(y·h) − NI(0), $1000), which collapses to m_i ≤ g_net_i. Markups m ~ Exponential(λ_c · s_i); s_i = prior-status weight (unemployed 5.0 / NILF 1.0 / disabled 0.15 / retired 0.15; CPS U→E vs N→E flow ratios, MMS 2013) × selection-probit propensity, mean-1 normalized within cell×reachable (weights change WHO enters, not totals). λ_c bisected so the weighted viable share hits the saturating person-level target mean(response_multiplier(eps_c, g_net, ceiling 1.5) − 1) over reachable. eps_ext_band (`cfg["matching"]`): single_mothers .25/.50/.65 (SSP-anchored upper), other_women .05/.20/.40 (two-channel provenance), men 0/.05/.15 (Paycheck-Plus-diluted upper, Atlanta-null lower). Subsidy modeled TAXABLE + COUNTABLE against means-tests (deliberate conservative choice). Calibration base = reachable non-employed pool, with an "employment-stock semantics" recalibration (target × E_c/R_c) as a sensitivity row.
4. **Hours**: entrants get quantile-matched hours (MPL percentile → incumbent hours percentile within cell; mean ≈ 770/yr); incumbent intensive margin (`02d::incumbent_hours_margin`) over eps_int_band {0.05/0.20/0.33} on the 10.3M eligible incumbents under 40 hrs/wk.
5. **Matching sim** (`02d`): gate reproduces the calibration identically by construction; Nash bargaining β {0.3/0.5/0.7}; $7.25 truncation binds for most entrants; net-basis pay-up grid across benefit cliffs (cliff diagnostic); sticky-incumbent-wage headline vs all-renegotiate bound; entrant firm capture = actual surplus (y − w_final)·h.
6. **Uncertainty variants** (all in `mpl_imputation_band.parquet` via `02f`, 9 variants on 4 axes, plus `entry_margin_band.parquet` rows): imputation {conditional/mills0/plain — now converged}; dispersion λ {0.50/1.00}; uniform offer penalty {10%/20%, Schmieder/KM-anchored}; status-mixture (left-skew) penalties {U5/N15/DR20 and U10/N25/DR30}; plus employment-stock base, take-up 0.80, spouse-employed zeroed/halved.

=== CURRENT HEADLINE (2026-07-09 run; verify from outputs) ===
Entry central **0.83M** (eps band 0.25–1.47M; dispersion 0.62–1.12M; uniform penalties 1.21/1.70M; skew mixtures 1.46/1.99M; estock 0.71M; take-up 0.66M; spouse-zeroed 0.69M). By group: sm 0.13 / ow 0.61 / men 0.09. By status: 38% unemployed, ~60% NILF-other, ~2% disabled+retired. Costs rigid-central $93.1B gross / $74.2B net (band $72–78B net across all models); firm capture 0.8–2.0% rigid, ≤54.8% flex (≤$160.3B). Clawback medians (g_net): sm 14.4% / ow 18.9% / men 14.9%. Cliff pay-up 0.115M (1 in 7 entrants). Hours margin 0.08/0.25/0.36M FTE vs entry 0.32M FTE. Reality anchor: the 1990s-EITC-alone precedent scaled to the reachable pool ≈ 0.9M — the central essentially matches it.

=== FILES TO READ (do not skip) ===
- Code: `code/00_setup/00_config.py`, `code/01_data_preparation/{01h,01i}*.py`, `code/02_descriptive_analysis/{02b,02d,02f}*.py`
- Docs: `docs/entry_from_nonemployment_methodology.md` (revision banner = full change log), `drafts/2026-07-09_technical-appendix.md`, `drafts/2026-07-08_wage-subsidy-impact-cost-summary.md`
- History: `Infrastructure/session_logs/2026-07-08_entry-remodel-delta-note.md` (every rebase attributed), `2026-07-08_replication_targets.md`; `Infrastructure/explorations/2026-07-09_entry-model-reality-assessment*.md` and the 2026-07-08 memos
- Outputs: `output/data/intermediate_results/population/{mpl_imputation_band,entry_margin_band,incumbent_hours_margin,matching_simulation,entry_reconciliation,_manifest}.{parquet,csv}`, `data/processed/nonemployed_pool_diagnostics.json`
- Literature: `Infrastructure/references/literature/catalog.yaml` (73+ entries; every load-bearing anchor primary-verified — spot-check, do not re-verify wholesale)

=== EVALUATION QUESTIONS (each deserves a verdict + evidence) ===
1. **Headline centering.** The central (0.83M) applies NO duration penalty, trims dispersion to λ=0.75, and inherits a σ estimated on ACCEPTED wages (offers truncated from below — so even λ=1.0 understates the low tail; no Flinn-Heckman-style correction exists in the build). Three independent conservatisms stack in one direction. Is 0.83M a defensible central, or is the evidence-weighted center closer to the status-mixture variant (1.46M)? Make a concrete recommendation with reasoning — this is the single most consequential open decision.
2. **Uncertainty architecture.** Nine variants on four MPL axes, plus the eps band, base semantics, take-up, and coordination rows — all varied one-at-a-time from the central. Is one-at-a-time honest here, or do the axes interact enough (penalties change reachability AND g_net AND composition; dispersion interacts with quantile-matched hours) that key combinations — or a proper joint/probabilistic treatment — are needed? Is the presentation a coherent story or a thicket? Propose the publication-grade structure.
3. **Double-counting risk in the men's cell.** The men's eps (0.05 central) was inherited from a literature reflecting a pool where ~half of NILF men have health barriers — but the status weights (disabled 0.15 etc.) now handle composition separately. Are we penalizing men twice (once in eps, once in weights)? Symmetrically: the W2c band-edge unit conversions (SSP/Paycheck-Plus arithmetic) were computed BEFORE the dispersion rebase changed g_net's scale — are the band edges still consistent with their own derivations?
4. **The saturation ceiling and extreme variants.** At pen20/skew-heavy, mean g_net rises substantially — check whether ceiling_ext (1.5) starts binding and whether the calibration behaves sensibly at the band extremes (targets vs realized in the diagnostics JSON).
5. **Hours margins.** PI-3 remains open: the entrant-hours rank-rank mapping (MPL percentile → hours percentile) has never been sensitivity-tested, and it drives the low marginal cost per entrant. Also assess whether the incumbent hours margin (eps_int on gross-g, 02b convention) should move to the net basis for consistency.
6. **Status weights sensitivity.** The entry-lottery weights (5.0/1.0/0.15/0.15) are disclosed judgment informed by flow ratios; no sensitivity has ever been run. How much does composition (and via hours/family-type, cost) move under plausible alternatives?
7. **Coherence of the two entry numbers.** 02b (EITC/CBO benchmark, 0.72M) now sits BELOW the structural central (0.83M) — the relationship flipped with the dispersion rebase. Is the reconciliation note (`entry_reconciliation.parquet` + draft section) still accurate and adequate?
8. **Standing limitations.** No demand side/displacement, no ramp-in dynamics, no take-up mechanism (scalar only), UI absent from NI(0), annual-vs-monthly timing, no fixed cost of work (queued re-pulls: SPLOC+NCHLT5, ASEC+SPM; ATUS deferred). Rank which now bind hardest given everything else that changed.
9. **Publication readiness.** The draft pair (`drafts/`) absorbed three headline rebases; the last full five-agent review predates all of them. Verify a sample of the draft's numbers against live outputs, then recommend the review/editorial sequence before anything ships.

=== CONSTRAINTS ===
- No fabricated citations, numbers, or data; `[unverified: ...]` markers per guardrails and `.claude/rules/ai-skeptic-rules.md`.
- No analysis-code changes in this evaluation (plan-first per `.claude/rules/plan-first-workflow.md`); deliverables are (a) a holistic evaluation report with per-question verdicts, (b) a recommended headline-centering decision memo for the user, (c) a ranked, effort-estimated fix list — as NEW dated files alongside existing artifacts, never overwriting them.
- Route to specialists in parallel where independent (`methodology-reviewer` for Q1–Q5, `literature-scout` only for gaps you actually find, review agents for Q9); verify quantitative claims yourself against the live outputs.
- Log incremental decisions to `Infrastructure/session_logs/`; end with a routing summary: confirmed / overturned / genuine user judgment calls / recommended next action.
