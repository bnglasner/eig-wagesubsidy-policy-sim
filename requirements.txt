# eig-wagesubsidy-policy-sim dependencies
# Python 3.12 required (see .python-version)
# Versions pinned to the validated local environment (2026-03-07).
# Update with: pip install -r requirements.txt --upgrade, then re-test.

# Interactive app
streamlit==1.55.0

# Data and visualization
pandas==2.3.3
numpy==2.4.2
plotly==5.24.1
pyarrow==23.0.1

# PolicyEngine microsimulation — development/pipeline only, not required for production.
# The app uses 204 pre-computed parquet schedules committed to the repo, so PolicyEngine
# is never called at runtime. Install separately for local pipeline work:
#   pip install policyengine-us==1.592.4
# policyengine-us==1.592.4
