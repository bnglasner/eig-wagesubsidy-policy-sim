# AI_START

Minimal, token-efficient startup for AI agents.

This file is the AI-only entry point for baseline setup. Human onboarding lives in `WORKSPACE/README.md`.

## Phase 1 (Only These Files)

Read:
1. `INFRA/docs/PROJECT_INTAKE.md`
2. `INFRA/AGENTS.md`

Then:
- Ask only missing BLOCKER questions from `INFRA/docs/PROJECT_INTAKE.md`.
- Do not read additional docs until BLOCKERs are complete.

## Phase 2 (After BLOCKERs Are Complete)

Read:
- `INFRA/docs/AI_BASELINE_APPLICATION_PLAYBOOK.md`
- `INFRA/docs/PIPELINE_LANGUAGE_PROFILE.md`
- `INFRA/docs/DATA_GOVERNANCE_POLICY.md`
- `INFRA/docs/STAGE_OUTPUT_CONTRACTS.md`

Then:
- Apply the baseline scaffold.
- Update placeholders in `INFRA/README.md` and `INFRA/CLAUDE.md`.
- Keep all paths aligned to the two-zone structure.

## Two-Zone Reminder

- `WORKSPACE/` is where human-facing code, inputs, and outputs live.
- `INFRA/` is where baseline code, docs, and automation live.
